export interface SettingsMenuItemProps {
    icon: string;
    text: string;
    onPress?: () => void;
}
export interface ProfileMenuItemProps {
  icon: string;
  text: string;
  onPress?: () => void;
}