# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Dose {
  """Hướng dẫn liều dùng từ y lệnh"""
  dosage_instructions: String
  due_at: DateTime!
  id: ID!

  """Tên thuốc được sao chép từ y lệnh"""
  medication_name: String!
  notify_at: DateTime!
  status: DoseStatus!
  taken_at: DateTime

  """Hướng dẫn cách dùng từ y lệnh"""
  usage_instructions: String

  """ID của người dùng sở hữu liều uống này"""
  user_id: ID!

  """ID của y lệnh từ bệnh viện"""
  ylenh_id: String!

  """Số thứ tự thuốc trong y lệnh"""
  ylenh_stt: String
}

enum DoseStatus {
  PENDING
  SKIPPED
  TAKEN
}

type Mutation {
  """Mô phỏng việc bệnh viện gửi yêu cầu đồng bộ y lệnh cho một bệnh nhân."""
  triggerReminderForPatient(mabn: String, ngay: String!, socmnd: String, sodienthoai: String): Boolean!
}

type Query {
  dosesByDateRange(endDate: String!, startDate: String!): [Dose!]!
  dosesByIds(dose_ids: [ID!]!): [Dose!]!
  pingSchedulingService: String!
}

type User {
  apn_tokens: [String!]
  diachi: String
  fcm_tokens: [String!]
  hoTen: String!
  id: ID!
  mabn: String!
  ngaysinh: String
  roles: [String!]!
  socmnd: String!
  sodienthoai: String!
}