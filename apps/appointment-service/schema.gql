# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Appointment {
  appointmentTime: DateTime!
  appointmentType: AppointmentType!
  clinic: Clinic
  doctor: Doctor
  id: ID!
  notes: String
  patientDob: String!
  patientGender: String!
  patientName: String!
  patientPhone: String!
  queueNumber: Int!
  status: AppointmentStatus!
}

enum AppointmentStatus {
  CANCELLED
  COMPLETED
  CONFIRMED
}

enum AppointmentType {
  CLINIC
  DOCTOR
}

input BookByClinicInput {
  appointmentTime: String!
  clinicId: ID!
  hospitalId: ID!
  notes: String
}

input BookByDoctorInput {
  appointmentTime: String!
  doctorId: ID!
  hospitalId: ID!
  notes: String
}

type Clinic {
  """Mã khoa phòng từ hệ thống bệnh viện"""
  externalMakp: String!

  """ID nội bộ của phòng khám"""
  id: ID!
}

enum ConnectionStatus {
  LINKED
  NOT_LINKED
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Doctor {
  avatarUrl: String
  bio: String

  """Phòng khám nơi bác sĩ làm việc"""
  clinic: [Clinic!]!
  id: ID!
  name: String!
}

input HospitalClinicInput {
  makp: String!
  tenkp: String!
}

type HospitalConnectionStatus {
  code: String!
  connectionStatus: ConnectionStatus!
  id: ID!
  name: String!
}

type Mutation {
  """Đặt lịch hẹn theo nhiều phòng khám."""
  bookAppointmentsByClinics(inputs: [BookByClinicInput!]!): [Appointment!]!

  """Đặt lịch hẹn theo nhiều bác sĩ."""
  bookAppointmentsByDoctors(inputs: [BookByDoctorInput!]!): [Appointment!]!

  """Hủy một lịch hẹn đã đặt."""
  cancelAppointment(id: ID!): Boolean!

  """
  Đồng bộ danh sách phòng khám từ dữ liệu bệnh viện (tạo mới, cập nhật, vô hiệu hóa).
  """
  upsertClinics(hospitalClinics: [HospitalClinicInput!]!): UpsertClinicResult!
}

type Query {
  """
  Lấy danh sách tất cả các phòng khám đang hoạt động để người dùng có thể đặt lịch.
  """
  activeClinics: [Clinic!]!

  """Lấy những bệnh viện đã được liên kết với tài khoản"""
  hospitalsWithConnectionStatus: [HospitalConnectionStatus!]!
  myAppointments: [Appointment!]!
}

type UpsertClinicResult {
  created: Int!
  deactivated: Int!
  updated: Int!
}